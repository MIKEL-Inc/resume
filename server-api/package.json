{
  "name": "server-api",
  "version": "0.0.0",
  "description": "GraphQL API endpoint and static Angular app server",
  "main": "server.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "watch": "rm -rf dist && tsc && tsc -w",
    "serve": "NODE_ENV=dev nodemon dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "graphql",
    "api",
    "resume",
    "angular",
    "static",
    "http-server"
  ],
  "author": "MIKEL, Inc.",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4",
    "express-graphql": "^0.7.1",
    "graphql": "^14.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/express-graphql": "^0.6.2",
    "@types/graphql": "^14.0.3",
    "nodemon": "^1.18.8",
    "typescript": "^3.2.2"
  }
}
